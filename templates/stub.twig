{% extends "layout.twig" %}

{% block title %}{{ title }} — Voyager Distance Tracker{% endblock %}

{% block content %}
<div class="card elev-sm error-card">
  <h6 style="color:var(--color-accent)">COMING SOON</h6>
  <h2>{{ title }}</h2>
  <p class="card-body">This page hasn't been designed yet. Check back once it has.</p>
  <a href="/" class="btn btn-secondary" style="align-self:center;margin-top:var(--space-2)">Back to overview</a>
</div>
{% endblock %}
